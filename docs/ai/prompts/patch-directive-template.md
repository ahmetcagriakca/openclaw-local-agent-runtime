# Patch Directive Template — Sprint {N}, Round {R}

<!--
Auto-generated by review-loop.sh after GPT HOLD.
Tells Claude Code exactly what to fix and how to prove it.
-->

## Source
- GPT Review: `docs/ai/reviews/S{N}-GPT-REVIEW.md`
- Round: {R}
- Previous verdict: HOLD

## Blockers to Resolve
- B1 — {description} [evidence: {what's wrong}]
- B2 — {description} [evidence: {what's wrong}]

## Required Patches
| Patch | Blocker | Target | Change |
|-------|---------|--------|--------|
| P1 | B1 | `{file}` | {change} |
| P2 | B2 | `{file}` | {change} |

## Per-Blocker Acceptance Criteria

### B1: {title}
- [ ] Patch applied: {what file, what change}
- [ ] Evidence command: `{exact command}`
- [ ] Evidence saved to: `evidence/sprint-{N}/{filename}`
- [ ] Evidence shows: {what output must contain}

### B2: {title}
- [ ] Patch applied: {what file, what change}
- [ ] Evidence command: `{exact command}`
- [ ] Evidence saved to: `evidence/sprint-{N}/{filename}`
- [ ] Evidence shows: {what output must contain}

## Constraints
1. No scope creep — only fix listed blockers
2. No history rewrite — no rebase on pushed commits
3. No unrelated changes
4. Run full test suite — no new failures
5. UNRESOLVABLE blockers: mark as UNRESOLVABLE in Section 12, document why

## After Patching
```bash
python -m pytest --tb=short -q
cd frontend && npx vitest run && cd ..
bash tools/blocker-resolution-check.sh {N}
git add -A && git commit -m "fix(s{N}): resolve R{R} review blockers" && git push
```
